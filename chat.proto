syntax = "proto3";

package chat;

service ChatService {
  rpc GetResponse (ChatRequest) returns (ChatResponse) {}
}

message ChatRequest {
  string message = 1;
}

message ChatResponse {
  string message = 1;
}